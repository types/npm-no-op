{
  "name": "@types/no-op",
  "typings": "index.d.ts",
  "files": [
    "index.d.ts"
  ],
  "version": "1.0.3",
  "scripts": {
    "lint": "tslint -p tsconfig.json --type-check *.ts **/*.ts",
    "test": "ts-node node_modules/blue-tape/bin/blue-tape \"spec/**/*.ts\" | tap-spec",
    "watch": "onchange -w \"**/*.ts\" -i -- npm -s run test"
  },
  "devDependencies": {
    "blue-tape": "^1.0.0",
    "dtslint": "^0.1.2",
    "no-op": "1.0.3",
    "onchange": "^3.2.1",
    "tap-spec": "^4.1.1",
    "ts-node": "^3.2.0",
    "tslint": "^5.5.0",
    "typescript": "^2.4.1"
  }
}
